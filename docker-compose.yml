version: '3.8'

services:
  db:
    image: pgvector/pgvector:pg16
    container_name: rumia_db
    environment:
      POSTGRES_USER: rumia
      POSTGRES_PASSWORD: rumia_password
      POSTGRES_DB: rumia
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: rumia_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

  # Usamos la imagen local cliproxy:dev que vi en tu sistema
  # cliproxy:
  #   image: cliproxy:dev
  #   container_name: rumia_cliproxy
  #   ports:
  #     - "3000:8317"
  #   volumes:
  #     - ./config.yaml:/CLIProxyAPI/config.yaml # Requiere tu archivo config
  #   restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
